@code {
    [Parameter] public string Type { get; set; }
    //[Parameter] public string Message { get; set; }

    [CascadingParameter]
    public DX_TexBox Parent { get; set; }

    protected override void OnInitialized()
    {
        Parent.AddValidator(this);
        base.OnInitialized();
    }
}